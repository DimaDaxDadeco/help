Clone git private: 
git clone https://username:password@github.com/NAME/repo.git
Reset uncommitted comments:
git reset --hard HEAD
Change end of line:
git rm -rf --cached .
git reset --hard HEAD
git reset --soft <hash последнего **не моего** коммита> - отменить коммит

REBASE:
git rebase --onto main-header HASH-LAST-COMMIT-MAIN-HEADER - назначить для ребейза head
